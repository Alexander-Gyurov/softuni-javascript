function solve(input){
    let startInterval = Number(input[0])
    let endInterval = Number(input[1])
    let buff = ""

for(let i = startInterval; i <= endInterval; i++){
    let numAsString = String(i);
    let startAsString = String(startInterval);
    let endAsString = String(endInterval);

    if(Number(numAsString[0]) >= Number(startAsString[0]) && Number(numAsString[0]) <= Number(endAsString[0]) ){

    }
}
}
solve(["2345", "6789"])